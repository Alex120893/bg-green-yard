import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { projects } from "@/lib/seo-pages";
export const metadata: Metadata = { title: "Нашите проекти | Озеленяване и поддръжка в София", description: "Реални обекти на BG.GREEN_YARD: озеленяване и поддръжка на зелени площи в София.", alternates: { canonical: "https://bg-greenyard.com/bg/proekti" } };
export default async function ProjectsPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (locale !== "bg") notFound(); return <main><section className="bg-white py-16 md:py-24"><div className="mx-auto max-w-6xl px-4 md:px-6"><p className="text-sm font-semibold uppercase tracking-[.16em] text-brand">BG.GREEN_YARD · София</p><h1 className="mt-3 text-4xl font-bold tracking-tight text-foreground md:text-5xl">Нашите проекти – озеленяване и поддръжка в София</h1><p className="mt-5 max-w-3xl text-lg text-muted">Реални обекти, за които полагаме професионална грижа.</p><div className="mt-12 grid gap-6 md:grid-cols-3">{projects.map((project) => <Link key={project.slug} href={`/bg/proekti/${project.slug}`} className="group overflow-hidden rounded-3xl bg-surface shadow-lg"><div className="relative aspect-[4/3]"><Image src={project.image} alt={`${project.name} — озеленяване и поддръжка в София`} fill className="object-contain p-6 transition-transform group-hover:scale-105" sizes="(min-width: 768px) 33vw, 100vw" /></div><div className="border-t border-border p-6"><h2 className="text-xl font-bold">{project.name}</h2><p className="mt-2 text-sm text-muted">{project.description}</p></div></Link>)}</div></div></section></main>; }
