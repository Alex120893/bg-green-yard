import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { posts } from "@/lib/seo-pages";
export const metadata: Metadata = { title: "Блог | Грижа за градини и тревни площи", description: "Практични съвети за градини, тревни площи, растения и поливни системи от BG.GREEN_YARD.", alternates: { canonical: "https://bg-greenyard.com/bg/blog" } };
export default async function BlogPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (locale !== "bg") notFound(); return <main><section className="bg-white py-16 md:py-24"><div className="mx-auto max-w-4xl px-4 md:px-6"><p className="text-sm font-semibold uppercase tracking-[.16em] text-brand">BG.GREEN_YARD · Съвети</p><h1 className="mt-3 text-4xl font-bold tracking-tight md:text-5xl">Блог за градини и зелени площи</h1><p className="mt-5 text-lg text-muted">Практични насоки за сезонната грижа за градината в София.</p><div className="mt-12 grid gap-5">{posts.map((post) => <article key={post.slug} className="rounded-3xl bg-surface p-7 shadow-sm"><h2 className="text-2xl font-bold"><Link href={`/bg/blog/${post.slug}`} className="hover:text-brand">{post.title}</Link></h2><p className="mt-3 leading-relaxed text-muted">{post.description}</p><Link href={`/bg/blog/${post.slug}`} className="mt-5 inline-flex font-semibold text-brand">Прочети статията →</Link></article>)}</div></div></section></main>; }
